# result 卡片

状态：2026-09-19，尚未通过真实钉钉设计器导入、发布及回调验收。
card.json 保存 UI；mapping.json 保存平台字段映射；preview.json 为无敏感预览数据。
发布后替换 mapping.json 的 template_id，并将映射配置到插件安装的 card 字段。
审批始终由平台校验真实成员权限和事件所属任务，模板按钮不授予权限。
